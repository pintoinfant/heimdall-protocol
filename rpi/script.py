import time
import sys

while True:
    # print(round(time.time()), end="", flush=True)  # No newline and flush immediately
    print(2, end="", flush=True)  # No newline and flush immediately
    time.sleep(5)